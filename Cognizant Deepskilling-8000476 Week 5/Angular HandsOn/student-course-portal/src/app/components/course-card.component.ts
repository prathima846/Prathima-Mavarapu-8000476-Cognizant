import {Component,Input,Output,EventEmitter,OnChanges,SimpleChanges} from '@angular/core';import {CommonModule} from '@angular/common';import {Course} from '../models/course';@Component({selector:'app-course-card',standalone:true,imports:[CommonModule],template:`<article class="card" [ngClass]="cardClasses" [ngStyle]="{borderLeftColor:borderColor}"><h3>{{course.name}}</h3><p>{{course.code}} · {{course.credits}} Credits</p><b [ngClass]="course.gradeStatus">{{course.gradeStatus}}</b><p *ngIf="isExpanded">{{course.description || 'Course details are available from the department.'}}</p><button (click)="isExpanded=!isExpanded">Show Details</button><button (click)="enrollRequested.emit(course.id)">Enroll</button></article>`})export class CourseCardComponent implements OnChanges{@Input({required:true})course!:Course;@Input()enrolled=false;@Output()enrollRequested=new EventEmitter<number>();isExpanded=false;ngOnChanges(c:SimpleChanges){console.log('Course changed',c['course'])}get cardClasses(){return {'card--enrolled':this.enrolled,'card--full':this.course?.credits>=4,expanded:this.isExpanded}}get borderColor(){return this.course?.gradeStatus==='passed'?'green':this.course?.gradeStatus==='failed'?'red':'grey'}}
