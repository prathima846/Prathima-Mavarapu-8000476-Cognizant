import {Component} from '@angular/core';import {FormsModule,NgForm,ReactiveFormsModule,FormControl,FormGroup,Validators} from '@angular/forms';@Component({standalone:true,imports:[FormsModule,ReactiveFormsModule],template:`<main><h1>Enrollment Request</h1><form #f="ngForm" (ngSubmit)="submit(f)"><input name="studentName" [(ngModel)]="model.name" required minlength="2" placeholder="Student name"><input name="studentEmail" [(ngModel)]="model.email" required email placeholder="Email"><input name="courseId" [(ngModel)]="model.courseId" required type="number" placeholder="Course ID"><select name="semester" [(ngModel)]="model.semester"><option>Odd</option><option>Even</option></select><label><input name="terms" [(ngModel)]="model.terms" required type="checkbox"> Agree to terms</label><button [disabled]="f.invalid">Submit</button></form><p>{{result}}</p></main>`})export class EnrollmentComponent{model={name:'',email:'',courseId:0,semester:'Odd',terms:false};result='';submit(form:NgForm){console.log(form.value,form.valid);this.result='Enrollment submitted successfully'}}
