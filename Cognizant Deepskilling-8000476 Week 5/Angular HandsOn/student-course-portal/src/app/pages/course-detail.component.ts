import {Component} from '@angular/core';import {CommonModule} from '@angular/common';import {ActivatedRoute} from '@angular/router';import {CourseService} from '../course.service';@Component({standalone:true,imports:[CommonModule],template:`<main><h1>Course Detail</h1><p *ngIf="course">{{course.name}} ({{course.code}}), {{course.credits}} credits</p><p *ngIf="!course">Course not found.</p></main>`})export class CourseDetailComponent{course;constructor(route:ActivatedRoute,service:CourseService){this.course=service.getCourseById(Number(route.snapshot.paramMap.get('id')))}}
