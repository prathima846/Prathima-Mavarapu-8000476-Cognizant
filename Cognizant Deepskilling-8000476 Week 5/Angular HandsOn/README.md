# Angular Hands-on Exercises

The PDF defines ten incremental exercises that build one application. The completed workspace is in `student-course-portal`; `docs/hands-on-checklist.md` maps its features to Hands-ons 1–10.

Run `npm.cmd install` then `npm.cmd start` inside `student-course-portal`.
