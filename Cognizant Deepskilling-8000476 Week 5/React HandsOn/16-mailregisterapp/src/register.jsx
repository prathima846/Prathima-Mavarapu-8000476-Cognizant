import{useState}from'react';export default function Register(){let[v,setV]=useState({name:'',email:'',password:''}),[msg,setMsg]=useState('');let change=e=>setV({...v,[e.target.name]:e.target.value});function submit(e){e.preventDefault();setMsg(v.name.length<5?'Name needs 5 characters':(!v.email.includes('@')||!v.email.includes('.'))?'Enter a valid email':v.password.length<8?'Password needs 8 characters':'Registration successful')}return <form onSubmit={submit}><h1>Mail Registration</h1>{['name','email','password'].map(x=><p><input name={x} type={x==='password'?'password':'text'} value={v[x]} onChange={change} placeholder={x}/></p>)}<button>Register</button><p>{msg}</p></form>}
