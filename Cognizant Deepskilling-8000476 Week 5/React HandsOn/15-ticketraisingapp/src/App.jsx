import{useState}from'react';export default function ComplaintRegister(){let[name,setName]=useState(''),[complaint,setComplaint]=useState('');function submit(e){e.preventDefault();alert('Complaint registered. Reference number: '+Date.now())}return <form onSubmit={submit}><h1>Raise a Complaint</h1><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Employee name"/><br/><textarea required value={complaint} onChange={e=>setComplaint(e.target.value)} placeholder="Complaint"/><br/><button>Submit</button></form>}
