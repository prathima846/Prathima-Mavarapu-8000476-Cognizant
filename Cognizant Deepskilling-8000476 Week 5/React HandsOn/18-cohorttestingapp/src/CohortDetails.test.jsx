import{describe,test,expect}from'vitest';import{render,screen}from'@testing-library/react';import CohortDetails from'./CohortDetails';import{CohortData}from'./Cohort';describe('Cohort Details Component',()=>{test('should create the component',()=>{render(<CohortDetails cohort={CohortData[0]}/>);expect(screen.getByRole('article')).toBeTruthy()});test('should initialize the props',()=>{render(<CohortDetails cohort={CohortData[0]}/>);expect(screen.getByText('React')).toBeTruthy()});test('should display cohort code in h3',()=>{render(<CohortDetails cohort={CohortData[0]}/>);expect(screen.getByRole('heading').textContent).toBe('ADM21JD001')})});
