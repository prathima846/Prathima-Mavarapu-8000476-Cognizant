import{describe,test,expect,vi}from'vitest';import axios from'axios';import GitClient from'./GitClient';vi.mock('axios');describe('Git Client Tests',()=>{test('should return repository names for techiesyed',async()=>{axios.get.mockResolvedValue({data:[{name:'repo-one'},{name:'repo-two'}]});await expect(new GitClient().getRepositories()).resolves.toEqual(['repo-one','repo-two'])})});
